## Description

Mark proposal items with review or implementation outcomes. This updates proposal state only and never applies source
changes.

## Inputs

### proposalPath

Optional artifact path. Omit to use the thread's latest finalized proposal. Must be `.agents/proposals/<name>.md`.
