## Description

Return compact status for the active proposal artifact without returning proposed file contents.

## Inputs

### proposalPath

Optional artifact path. Omit to use the thread's active proposal. Must be `.agents/proposals/<name>.md`.
